# Optional extras. The default install (requirements.txt) already gives you dense and
# hybrid retrieval via the LSA backend, which downloads nothing.
#
# Install these to swap in a pretrained bi-encoder and a cross-encoder reranker:
#
#     pip install -r requirements-ml.txt
#     EMBEDDING_MODEL=BAAI/bge-small-en-v1.5 RERANK_ENABLED=true uvicorn app.main:app
#
# Both models are ~130MB and run on CPU. They are downloaded from HuggingFace on first
# use, so this path needs network access at startup; the LSA backend does not.
sentence-transformers==3.3.1
torch==2.5.1

# Offline fine-tuning pipeline (scripts/finetune_*.py). Not imported by the serving path.
transformers==4.47.1
peft==0.14.0
trl==0.13.0
datasets==3.2.0
